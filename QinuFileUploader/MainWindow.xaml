<Window
    x:Class="QinuFileUploader.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QinuFileUploader"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    
    
    mc:Ignorable="d">

    <Grid Name="MainFrame">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

        </Grid>

        <Grid  Margin="0,10" Width="420" Grid.Row="1" 
				   HorizontalAlignment="Left" 
				   VerticalAlignment="Bottom">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBlock Text="模糊查询" VerticalAlignment="Center"></TextBlock>
            <TextBox x:Name="CurrentText" 
					  Margin="10,10,0,10"
					 Grid.Row="0"
					 Grid.Column="1"

						 Height="20" 
						 VerticalAlignment="Stretch" TextWrapping="Wrap">
            </TextBox>
            <Button Grid.Column="2" Content="查询" Command="{Binding SearchCommand}" Margin="10" Width="120"></Button>

        </Grid>
        <GridView
            x:Name="BasicGridView"
            IsItemClickEnabled="True"
            ItemsSource="{Binding FileInfos}"
            SelectionMode="Single">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding}" Width="90" Height="90" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Window>
